;;; list.el --- -*- lexical-binding: t -*-
;;; Commentary:
;;; Code:

(defconst core/module-list
  '("config/disable"
    "config/electric"
    "config/frame"
    "config/line-numbers"
    "config/custom"

    "bind-key"
    "evil"

    "theme"
    "ivy")
  "The list of modules that exist.")

;;; list.el ends here
